// Shared types for API implementations - re-exports from lib/types
// PHASE 2: Replace src/services/authService.ts with Supabase wrapper - no other changes to callsites required.

export * from '@/lib/types';
